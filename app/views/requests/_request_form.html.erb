<%= simple_form_for [sock, request] do |f| %>
  <%= f.input :user_msg, label: "Laissez un message..." %>
  <%= f.input :start_date, as: :date, label: "Date de début" %>
  <%= f.input :end_date, as: :date, label: "Date de fin" %>
  <%= f.submit "Envoyer", class: "btn btn-ghost" %>
  <% end %>
